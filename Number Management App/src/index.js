import React from 'react';
import ReactDOM from 'react-dom';
import NumberManagementApp from './App';

ReactDOM.render(
  <React.StrictMode>
    <NumberManagementApp />
  </React.StrictMode>,
  document.getElementById('root')
);